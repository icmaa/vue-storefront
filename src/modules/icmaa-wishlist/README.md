# `icmaa-wishlist` module

* Extend the `wishlist` module and add peristence.

## Configs

You need to add the following config to the `config/local.json`:

```
  ...
  "icmaa_wishlist": {
    "endpoint": "/api/icmaa-wishlist"
  },
  ...
```

## Todo

[ ] ...
